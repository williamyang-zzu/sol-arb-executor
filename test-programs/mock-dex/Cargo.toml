[package]
name = "mock-dex"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "=2.2.1"
spl-token = { version = "=7.0.0", features = ["no-entrypoint"] }

